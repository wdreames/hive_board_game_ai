default:
    image: python:latest
stages:
    - test
test:
    image: python:latest
    script:
        - which python
        - which python3
        - python3 --version
        - coverage run -m unittest test/test_*.py
        - coverage report -m